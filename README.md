# LoRa Мульти-Контроллер

Беспроводная система дистанционного управления на базе радиомодулей LoRa для управления реле на расстоянии до нескольких километров.

![Тест связи 1](тест1.jpg)
![Тест связи 2](тест2.jpg)
![Сборка RX](СборкаRX.jpg)
![Сборка TX](СборкаTX.jpg)

![Версия](https://img.shields.io/badge/Version-3.3-blue)
![Платформа](https://img.shields.io/badge/Platform-ESP8266%20%7C%20Arduino%20Nano-orange)
![Связь](https://img.shields.io/badge/LoRa-868%2F915MHz-green)

---

## 📋 Описание проекта

**LoRa Мульти-Контроллер** — это система беспроводного управления, состоящая из двух компонентов:

| Компонент | Платформа | Назначение |
|-----------|-----------|------------|
| **TX (Передатчик)** | ESP8266 (WeMoS D1 mini) | Центральный контроллер с веб-интерфейсом и WiFi |
| **RX (Приёмник)** | Arduino Nano | Исполнительное устройство с реле |

Система обеспечивает надёжную связь на расстоянии до **15 км** (в зависимости от условий) без использования интернета или сотовой связи.

### 🎯 Основные возможности

- **Управление до 10 приёмников** одновременно через единый интерфейс
- **Веб-интерфейс** на русском языке для управления всеми устройствами
- **Обновление прошивки по воздуху (OTA)** без физического подключения
- **Мониторинг состояния** в реальном времени
- **Два режима работы WiFi**: точка доступа или подключение к существующей сети
- **Защищённый доступ** с сессиями и авторизацией
- **Индикация состояния связи** с каждым приёмником через WS2812 RGB светодиод
- **Автоматическая синхронизация времени** через NTP (сервер 192.168.*, GMT+)
- **Автоматическое повторное отправление команд** при сбоях связи (до 5 попыток, таймаут 10 сек)
- **Мониторинг фрагментации памяти** с автоматической перезагрузкой при критических значениях (порог 80%)
- **Расширенная консоль** с цветным выводом и автоматическим обновлением каждые 2 секунды
- **Система сессий** для защищённого доступа с уникальными ID сессий
- **Блокировка управления>** Автоматическая блокировка/разблокировка кнопок управления на время отправки команд для избежания повторной сработки 

---

## 🏗️ Архитектура системы

```
┌─────────────────────────────────────────────────────────────┐
│                        TX (ESP8266)                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐   │
│  │   WiFi AP    │  │  Web Server  │  │   LoRa TX        │   │
│  │  (AP/Client) │  │   :80 port   │  │   (UART)         │   │
│  └──────────────┘  └──────────────┘  └──────────────────┘   │
│                            │                                │
│                            ▼                                │
│                   ┌──────────────────┐                      │
│                   │   Конфигурация   │                      │
│                   │   Приёмников     │                      │
│                   │   (до 10 шт)     │                      │
│                   └──────────────────┘                      │
└─────────────────────────────────────────────────────────────┘
                            │ LoRa (868/915 МГц)
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                        RX (Arduino Nano)                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐   │
│  │   LoRa RX    │  │   Реле       │  │   Кнопка TEST    │   │
│  │   (UART)     │  │   (PIN 5)    │  │   (PIN 6)        │   │
│  └──────────────┘  └──────────────┘  └──────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 📦 Аппаратные требования

### Передатчик (TX)

| Компонент | Характеристики |
|-----------|----------------|
| Плата | WeMoS D1 mini (ESP8266) |
| Радиомодуль | DX-LR03-900T30S |
| Питание | 5В через USB |
| Антенна | штырь 868 МГц (входит в комплект модуля) |

### Приёмник (RX)

| Компонент | Характеристики |
|-----------|----------------|
| Плата | Arduino Nano (ATmega328P) |
| Радиомодуль | DX-LR03-900T30S |
| Реле | 220В  |
| Кнопка | Для ручного теста связи |
| Питание | 5В через USB или адаптер |

### Схема подключения

#### TX (ESP8266 WeMoS → LoRa)

| WeMoS Pin | LoRa Module Pin | Назначение |
|-----------|-----------------|------------|
| D1 (GPIO5) | TX | Приём данных от LoRa |
| D2 (GPIO4) | RX | Передача данных в LoRa |
| 5V | VCC | Питание 5В |
| GND | GND | Общий провод |

**Примечание**: Используется аппаратный UART (Serial) для связи с LoRa модулем, поэтому при прошивке модуль должен быть отключен от платы.

#### RX (Arduino Nano → LoRa)

| Nano Pin | LoRa Module Pin | Назначение |
|----------|-----------------|------------|
| Pin 0 (RX) | TX | Приём данных от LoRa |
| Pin 1 (TX) | RX | Передача данных в LoRa |
| Pin 5 | RELAY | Управление реле |
| Pin 6 | BUTTON | Кнопка теста связи |
| Pin 4 | WS2812 | Управление светодиодом WS2812 |
| От блока питания + | VCC | Питание 5В | 
| От блока питания - | GND | Общий провод |

**Примечание**: Используется аппаратный UART (Serial) для связи с LoRa модулем, поэтому при прошивке модуль должен быть отключен от платы.

---

## 💻 Программные требования

### Среда разработки

- **Arduino IDE** версии 1.8.x или 2.x
- **Платы ESP8266** (инструменты → платы → ESP8266 Boards)
- **Платы Arduino AVR Boards** (для Nano)

### Установка плат в Arduino IDE

1. Открыть **Файл → Настройки**
2. В поле **Дополнительные ссылки для менеджера плат** добавить:
   ```
   http://arduino.esp8266.com/stable/package_esp8266com_index.json
   ```
3. Открыть **Инструменты → Плата → Менеджер плат**
4. Установить **esp8266 by ESP8266 Community**

### Библиотеки

Для ESP8266 (встроены в ядро):
- `ESP8266WiFi`
- `ESP8266WebServer`
- `EEPROM`
- `ESP8266mDNS`
- `WiFiUdp`
- `ArduinoOTA`
- `NTPClient` (для синхронизации времени)

Для Arduino Nano:
- `Adafruit_NeoPixel` (для управления WS2812 RGB светодиодом)

---

## 🚀 Установка и прошивка

### Прошивка TX (ESP8266 WeMoS D1 mini)

1. Открыть файл `TX_Last.ino` в Arduino IDE
2. Выбрать плату: **WeMos D1 R2 & mini** или **LOLIN(WEMOS) D1 R2**
3. Выбрать порт подключения
4. Нажать **Загрузить** (Ctrl+U)

### Прошивка RX (Arduino Nano)

1. Открыть файл `RX_Last.ino`
2. Выбрать плату: **Arduino Nano**
3. Выбрать процессор: **ATmega328P** (Old Bootloader при необходимости)
4. Выбрать порт подключения
5. Нажать **Загрузить** (Ctrl+U)

### OTA-обновление (только TX)

Прошивка TX поддерживает обновление по воздуху через веб-интерфейс:

1. Подключиться к WiFi точки доступа `LoRaTransmitterAP` (пароль: `*`)
2. Открыть в браузере `http://192.168.4.1`
3. Авторизоваться: логин `admin`, пароль `*`
4. Перейти на вкладку **OTA**
5. Загрузить `.bin` файл прошивки

### Первый запуск

1. Подать питание на TX — создаст точку доступа `LoRaTransmitterAP`
2. Подключиться к WiFi сети (пароль: `12345678`)
3. Открыть в браузере `http://192.168.4.1`
4. Авторизоваться: логин `admin`, пароль `*`

---

## 📖 Инструкция по использованию

### Веб-интерфейс

После подключения к устройству доступны следующие страницы:

| Страница | URL | Описание |
|----------|-----|----------|
| Авторизация | `/login` | Вход в систему |
| Главная | `/` | Статус всех приёмников, консоль |
| WiFi настройки | `/wifi_settings` | Настройки WiFi подключения |
| Управление приёмниками | `/receiver_management` | Добавление/удаление/редактирование приёмников |
| OTA обновление | `/ota_update` | Обновление прошивки по воздуху |

### Дополнительные возможности

- **Система сессий**: защищённый доступ с уникальными ID сессий
- **Автоматическая синхронизация времени**: NTP клиент для отображения времени в логах (сервер: 192.168.*)
- **Автоматическое повторное отправление команд**: при сбоях связи команды автоматически переотправляются до 5 раз
- **Мониторинг состояния памяти**: проверка фрагментации кучи и автоматическая перезагрузка при критических значениях
- **Расширенная консоль**: цветной вывод с автоматическим обновлением каждые 2 секунды

### Управление реле

На главной странице для каждого приёмника доступны кнопки:

- ⚡ **Вкл. реле** — включить реле
- 🔌 **Выкл. реле** — выключить реле
- 📡 **Тест связи** — проверить соединение с приёмником

### Консоль

Консоль отображает:
- Отправленные команды
- Полученные ответы
- Статус приёмников
- Ошибки связи

Автоматическое обновление каждые 2 секунды.

### Настройка WiFi

1. Перейти на страницу **WiFi**
2. Включить **Режим клиента**
3. Выбрать сеть из списка или ввести вручную
4. Ввести пароль
5. Нажать **Сохранить** — устройство перезагрузится

После перезагрузки устройство подключится к указанной сети и получит IP-адрес.

### Добавление приёмников

1. Перейти на страницу **Приемники**
2. Нажать **+ Добавить приемник**
3. Заполнить:
   - **Имя** — понятное название (например, "Гараж")
   - **Адрес** — HEX адрес приёмника (01, 02, 0A, FF и т.д.)
   - **Включен** — галочка для активации
4. Нажать **Сохранить настройки**

> ⚠️ **Важно**: Адрес приёмника в коде RX должен совпадать с адресом, указанным в конфигурации TX.

### OTA обновление прошивки

1. Перейти на страницу **OTA**
2. Выбрать `.bin` файл новой прошивки
3. Нажать **Загрузить и обновить**
4. Дождаться завершения обновления (устройство перезагрузится)

> ⚠️ **Не отключайте питание** во время обновления!

---

## 📡 Протокол связи

### Формат команд TX → RX

```
[XX]COMMAND
```
- `XX` — адрес приёмника в HEX (01, 02, ..., FF)
- `COMMAND` — команда

| Команда | Описание | Ответ от RX |
|---------|----------|-------------|
| `RON` | Включить реле | `OK1` |
| `ROF` | Выключить реле | `OK0` |
| `TST` | Тест связи | `TSTOK` |

### Формат ответов RX → TX

```
[XX]STATUS
```
- `XX` — адрес приёмника в HEX
- `STATUS` — `STA1` (реле включено) / `STA0` (выключено)

### Адресация

- **TX Address**: `0x00` (широковещательный)
- **RX Address**: настраивается в коде (от `0x01` до `0xff`)

---

## ⚙️ Конфигурация

### Параметры по умолчанию

#### WiFi TX

| Параметр | Значение по умолчанию |
|----------|----------------------|
| AP SSID | `LoRaTransmitterAP` |
| AP Пароль | `12345678` |
| Web логин | `admin` |
| Web пароль | `*` |

#### Параметры LoRa

| Параметр | Значение |
|----------|----------|
| Скорость UART | 9600 baud |
| Частота | 868 МГц |
| Таймаут приёмника | 5 минут |

#### Приёмники

По умолчанию в EEPROM TX хранятся конфигурации двух приёмников:

| # | Адрес | Имя | Включён |
|---|-------|-----|---------|
| 1 | 0x01 | Receiver 1 | Да |
| 2 | 0x02 | Receiver 2 | Да |

Максимум — 10 приёмников (адреса 0x01–0xFF).

### Изменение адреса приёмника

В файле `RX_Last.ino`:

```cpp
#define MY_ADDRESS     0x02  // Изменить на нужный адрес
```

---

## 🛠️ Диагностика и устранение неполадок

### TX не видит приёмники

- Проверить подключение LoRa модуля (RX→TX, TX→RX)
- Проверить совпадение адресов в TX и RX
- Проверить питание LoRa модуля (5В)

### OTA обновление не работает

- Убедиться, что выбран `.bin` файл, а не `.ino`
- Проверить стабильность питания во время обновления
- Проверить стабильность Wi-Fi во время обновления

### Веб-интерфейс недоступен

- Проверить, что IP соответствует режиму WiFi
- В режиме AP: `http://192.168.4.1`
- В режиме клиента: IP из веб-интерфейса роутера

### Веб-интерфейс сбоит, сбои в настройках приемников, сбои в работе Wi-Fi

- Прошить на TX_Reset_to_default.bin (сбрасывает настройки приемников и wi-fi)
- Настроить Wi-Fi и приемники
- Прошить на TX_Last.ino.bin

## 💡 Индикация состояний RX (WS2812)

### Цветовая индикация светодиода WS2812

| Состояние | Цвет | Описание |
|-----------|------|----------|
| Реле включено | 🔴 Красный | Постоянное свечение |
| Реле выключено | ⚫ Выключено | Светодиод погашен |
| Ожидание ответа на тест | 🟡 Жёлтый | Мигание с интервалом 500мс |
| Режим ожидания | 🔵 Синий | Медленное мигание каждые 2 секунды |
| Успешное выполнение команды | 🟢 Зелёный | Включение на 2 секунды |
| Ошибка/таймаут | 🔴 Красный | Быстрое мигание 5 раз |

### Сигналы при старте

При включении питания RX выполняет тестовую последовательность:
1. Синий цвет (500мс)
2. Зелёный цвет (500мс) 
3. Выключение

Это подтверждает работоспособность WS2812 светодиода.

## 🔧 Дополнительные возможности TX

### Автоматическое повторное отправление команд

TX автоматически повторно отправляет команды при сбоях связи:
- **Количество попыток**: до 5 раз
- **Таймаут ответа**: 10 секунд
- **Задержка между попытками**: 5 секунд

### Мониторинг фрагментации памяти

TX автоматически проверяет фрагментацию кучи:
- **Порог предупреждения**: 80%
- **Интервал проверки**: 30 секунд
- **Автоматическая очистка**: по возможности
- **Автоматическая перезагрузка**: при критических значениях

### NTP синхронизация времени

TX автоматически синхронизирует время через NTP:
- **Сервер**: 192.168.** (локальный, настраивается через прошивку)
- **Часовой пояс**: (настраивается через прошивку)
- **Обновление**: каждые 60 секунд
- **Отображение**: в логах консоли

## 🧪 Тестирование

### Проверка связи TX ↔ RX

1. Прошить оба устройства с отключенным LoRa модулем
2. Отключить прошитые устройства и подключить к ним LoRa модуль
3. В веб-интерфейсе TX нажать **Тест связи** для приёмника
4. В консоли веб-интерфейса TX должно появиться сообщение `✅ Тест связи подтвержден приемником ...`

### Проверка управления реле

1. В веб-интерфейсе TX нажать **Вкл. реле** / **Выкл. реле**
2. В консоли веб-интерфейса TX должно появиться: `✅ Реле приемника ... успешно включено/выключено`
3. Реле должно физически переключаться

### Проверка RX ↔ TX

1. Нажать кнопку в корпусе RX
2. Индикация начнет мигать желтым
3. При успешной связи с TX загорится зеленым (на 2 сек)

---

## 🚀 Перспективы развития

### Запланированные функции

- [ ] Добавить датчики температуры/напряжения на RX
- [ ] Реализовать энергосбережение для батарейных RX/TX
- [ ] Переход TX на ESP32-C6

### Аппаратные улучшения

- **ESP32** — больше памяти, Bluetooth, два ядра
- **Солнечное питание** — автономные RX станции

---

## 📄 Лицензия

**Пообъектная именная лицензия**

**Автор**: Листопад Денис уч. Сисим Дражный

---

## 🤝 Поддержка

При возникновении вопросов:

1. Проверьте раздел [Диагностика](#-диагностика-и-устранение-неполадок)
2. Изучите консоль на наличие ошибок
3. Проверьте подключение оборудования

---

## 📁 Структура проекта

```
.
├── README.md                         # Пользовательская документация
├── TX_Last/
│   └── TX_Last.ino                   # Прошивка передатчика (ESP8266)
└── RX_Last/
    └── RX_Last.ino                   # Прошивка приёмника (Arduino Nano)
```

### Описание файлов

| Файл | Назначение | Платформа |
|------|------------|-----------|
| `RX_Last/RX_Last.ino` | Прошивка приёмника: приём команд по LoRa, управление реле, WS2812 индикация, отправка статуса | Arduino Nano |
| `TX_Last/TX_Last.ino` | Прошивка передатчика: веб-интерфейс, WiFi, OTA, управление до 10 приёмников, NTP синхронизация | ESP8266 (WeMoS) |

---

*Документация обновлена: 18 января 2026 г.*